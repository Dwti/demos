<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yanxiu.com.yxyl"
    android:versionCode="1"
    android:versionName="V1.0">
    <uses-permission android:name="android.permission.INTERNET">
    </uses-permission>
    <uses-permission android:name="android.permission.CALL_PHONE">
    </uses-permission>
    <!-- android:theme="@style/AppTheme" -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".JoinClassActivity" />
        <activity android:name=".CommitNameActivity" />
        <activity android:name=".GuideActivity" >

        </activity>
        <activity
            android:name=".BasepageActivity"
            android:label="@string/title_activity_basepage"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".CompleteMessageActivity" />
        <activity android:name=".ChooseRegionActivity" />
        <activity android:name=".ChooseSectionActivity" />
        <activity android:name=".ChooseSchoolActivity"></activity>
    </application>

</manifest>